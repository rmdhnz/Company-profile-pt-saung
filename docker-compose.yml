services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: company-profile-dev

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - CHOKIDAR_USEPOLLING=true

    command: pnpm dev --host 0.0.0.0